$(document).ready(function () {
    var gallery = $("body").gallery({
        menu: true,
         cards: true,
        heading: true,
        transform: 'scale(1.3)',
        downArrow: true,
        menuItem: true

    });

});